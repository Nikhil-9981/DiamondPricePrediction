1. conda create -p venv python==3.8 
2. cls --> use for clear the terminal 
3. conda activate venv/
4. pip install -r requirement.txt
5. python  setup.py install                          # it will install setup.py